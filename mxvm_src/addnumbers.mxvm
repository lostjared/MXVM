program AddNumbers {
	section data {
		ptr pointer = null
		int x = 0xFF
		int y = 2
		float result = 0
		float f = 10.0
		string value = "%f + %d = %f %p"
		string fmt = " value is; %d\n"
		
	}

	section code {
	start:
	   alloc pointer, 8, 3
	   store x, pointer, 5
	   load x, pointer, 1
	   mov y, x
	   print fmt, y
	stop:
		free pointer
		exit 0 
	}
}